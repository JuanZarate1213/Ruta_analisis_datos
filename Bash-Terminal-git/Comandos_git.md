git config --global user.name
git config --global user.email <estos comandos los usamos para configurar la firma> 
<que van a llevar cada una de las acciones en el repositorio>

git init <inicia git en el directorio para empezar a hacer la documentación de los cambios>

git commit -m <comenta sin necesidad de abrir el editor>

git status <muestra el estado de cada uno de los archivos en el directorio, si se>
< subieron, si se modificaron o si aún no se han subido>

git log <Muestra el historial de lo que se ha hecho junto con los commits y el autor>

> git config --gobal alias "NOMBRE DEL COMANDO"

git rm --cached <file> <para bajar el archivo de la mesa de preparación>

git checkout <restaura un archivo has ta el último add que se le realizó>

git reset --hard <restaura toda la carpeta hasta el último commit que se le hizo>

git diff <muestra lso cambios que se han hecho a los documentos>

